@model IEnumerable<knjiznica.Models.Autor>
<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.autorID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.prezime)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ime)
        </th>
        
        <th></th>
    </tr>
    @foreach (var item in Model)
    {
        <tr>
            <td>@Html.DisplayFor(modelItem => item.autorID)</td>
            <td>@Html.DisplayFor(modelItem => item.prezime)</td>
            <td>@Html.DisplayFor(modelItem => item.ime)</td>
            
            <td>
                @Html.ActionLink("Edit", "Edit", new { autorID = item.autorID }) |
                @Html.ActionLink("Delete", "Delete", new { autorID = item.autorID })
            </td>
        </tr>
    }
</table>